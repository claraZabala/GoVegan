<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.govegan">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.CAMERA">


    </uses-permission>
    <application
        android:allowBackup="true"
        android:icon="@drawable/icono"
        android:label="@string/app_name"
        android:roundIcon="@drawable/icono_circular"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:fullBackupContent="@xml/backup_descriptor">
        <activity android:name=".vista.Forum"/>
        <activity android:name=".vista.CalendariSetmanal" />
        <activity android:name=".vista.LlistaCompra"/>
        <activity android:name=".vista.Recepta"/>
        <activity android:name=".vista.PaginaPrincipal"/>
        <activity android:name=".vista.LayoutCuriositats"/>
        <activity android:name=".vista.LayoutPropostes"/>
        <activity android:name=".vista.RecuperarContra"/>
        <activity android:name=".vista.AfegirProposta"/>
        <activity android:name=".vista.SignUp"/>
        <activity android:name=".vista.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>

